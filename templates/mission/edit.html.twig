{% extends 'base.html.twig' %}

{% block title %}Edit Mission{% endblock %}

{% block body %}
<div class="p-4">
    <div class="table">
        <h1 class="h3 mb-3 fw-bold">Modifier la mission: {{ mission.nom }}</h1>

        {{ include('mission/_form.html.twig', {'button_label': 'modifier'}) }}

        <a class="btn btn-secondary"  href="{{ path('app_mission_index') }}">retour</a>

        {{ include('mission/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
