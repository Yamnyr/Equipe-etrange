{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}

<div class="p-4">

    <h1 class="h3 mb-3 fw-bold">{{ classe.nom }}</h1>
        <div class="table">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ classe.id }}</td>
                    </tr>
                    <tr>
                        <th>Nom</th>
                        <td>{{ classe.nom }}</td>
                    </tr>
                    <tr>
                        <th>Image</th>
                        <td>{{ classe.image }}</td>
                    </tr>
                </tbody>
            </table>
        </div>

    <a class="btn btn-secondary"  href="{{ path('app_classe_index') }}">retour</a>

    <a class="btn btn-secondary"  href="{{ path('app_classe_edit', {'id': classe.id}) }}">modifier</a>

    {{ include('classe/_delete_form.html.twig') }}
</div>
{% endblock %}
